[project]
name = "rete-dashboard"
version = "0.1.0"
description = "Manage devices on local network"
authors = [{ name = "alv67" }]
requires-python = ">=3.9"
dependencies = [
    "flask"
]

[tool.uv]
# If you want to use lockfile, you can also enable this